import './Header.css';
import Logo from './components/Logo/Logo';

function Header() {
    return (
        <header className='header'>
            <Logo />
        </header>
    );
}

export default Header;